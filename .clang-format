# .clang-format
# Optimized for Competitive Programming
# Base style: Google (Excellent default handling for C++ templates and pointers)

Language: Cpp
BasedOnStyle: Google

# ==============================================================================
# Core Spacing & Line Wrapping
# ==============================================================================

# Prevents Clang from arbitrarily wrapping long lines. In CP, you don't want
# complex math, nested templates, or long I/O statements broken across multiple
# lines just because they hit an arbitrary 80-character limit.
ColumnLimit: 0

# Overrides Google's 2-space indent to 4 spaces. This creates a stronger visual
# hierarchy, which is crucial when quickly scanning deeply nested loops (e.g.,
# multi-dimensional DP, DFS/BFS graph traversals).
IndentWidth: 4
TabWidth: 4

# Pulls public/private modifiers flush with the class definition
AccessModifierOffset: -4

# ==============================================================================
# Vertical Density & Cleanup
# ==============================================================================

# Collapses multiple blank lines into a single blank line. If you hit 'Enter' a
# bunch of times while thinking, this snaps the file back into a dense, readable
# block upon formatting.
MaxEmptyLinesToKeep: 1

# Removes extraneous blank lines immediately after an opening brace.
# Prevents wasted vertical space at the start of functions or loops.
KeepEmptyLinesAtTheStartOfBlocks: false

# ==============================================================================
# Single-Line Structures (Speed & Screen Real Estate)
# ==============================================================================

# Allows empty blocks `{}` to remain on a single line.
AllowShortBlocksOnASingleLine: Empty

# Allows short, single-statement functions to stay on one line.
# Excellent for custom comparators, struct constructors, or simple getters.
AllowShortFunctionsOnASingleLine: All

# Allows single-line lambdas (very common in modern C++ CP for custom sorting).
AllowShortLambdasOnASingleLine: All

# Allows `if (condition) statement;` on a single line to save vertical space.
# Uses 'WithoutElse' to ensure if/else chains still break to new lines,
# preventing dangling-else logic bugs during high-pressure contests.
AllowShortIfStatementsOnASingleLine: WithoutElse

# Allows single-statement loops on one line.
# Example: `for(int i=0; i<n; i++) cin >> a[i];` stays perfectly compact.
AllowShortLoopsOnASingleLine: true

# ==============================================================================
# Alignment & Aesthetics
# ==============================================================================

# Automatically aligns the values of consecutive `#define` statements.
# Turns messy CP boilerplate macros into a neat, easily readable column.
AlignConsecutiveMacros: true

# Automatically aligns chained inline comments (`//`) at the ends of lines.
# Keeps variable annotations neatly organized on the right-hand side.
AlignTrailingComments: true

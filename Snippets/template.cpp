#include <bits/stdc++.h>
using namespace std;

#ifdef LOCAL
#include "tl.hpp"
#else
#define dbg(...) 14122021
#endif

#define int long long

template <typename T>
bool chmax(T &x, T y) {
    return x < y and (x = y, true);
}

template <typename T>
bool chmin(T &x, T y) {
    return x > y and (x = y, true);
}

signed main() {
    cin.tie(nullptr)->sync_with_stdio(false);

    // The Legend Himself
}

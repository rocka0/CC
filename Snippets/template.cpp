#ifdef LOCAL
#include "tl.hpp"
#else
#include <bits/stdc++.h>
using namespace std;
#define debug(...) 14122021
#endif

template <typename T>
bool chmax(T &x, T y) {
    return x < y and (x = y, true);
}

template <typename T>
bool chmin(T &x, T y) {
    return x > y and (x = y, true);
}

int main() {
    cin.tie(nullptr)->sync_with_stdio(false);

    // The Legend Himself
}

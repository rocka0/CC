{
    "shell_cmd": "g++ -DLOCAL -std=c++17 -O2 ${file_name} -o ${file_base_name} && time ./${file_base_name} < in > out && rm ./${file_base_name}",
    "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
    "working_dir": "${file_path}",
    "selector": "source.c++",
    "quiet": true
}